# Deepgram — used server-side only to mint short-lived tokens for the browser.
# Get one at https://console.deepgram.com/
DEEPGRAM_API_KEY=

# Groq — used server-side for translation.
# Get one at https://console.groq.com/keys
GROQ_API_KEY=
